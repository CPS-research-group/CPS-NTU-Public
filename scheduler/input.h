#include <iostream>
#include <vector>
#include <assert.h>
#include <fstream>
#include <string>

using namespace std;

string path_to_test_case = "../test_case/";
string path_to_input = "../input/";

int no_input, no_tasks, no_ap_upload, no_server, no_ap_download, max_viol_allowed;
extern vector <int> proc_time_s1, proc_time_s2, proc_time_s3;
